name: Docker Build and Publish

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  # Hardcoded lowercase repository name to prevent Docker push errors
  IMAGE_NAME: vincent-pro-ai/open-turboquant

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Atomic Host Cleanup & Docker Reconfiguration
        run: |
          # Brute force cleanup of the biggest offenders on the system partition
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache
          
          # Reconfigure Docker to use the /mnt partition directly
          sudo systemctl stop docker.service
          sudo systemctl stop docker.socket
          sudo mkdir -p /mnt/docker-data
          echo '{"data-root": "/mnt/docker-data"}' | sudo tee /etc/docker/daemon.json
          sudo systemctl start docker.service
          df -h

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          # Use standard docker build with multiple tags for platform compatibility
          REGISTRY_PATH=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          docker build -t $REGISTRY_PATH:latest -t $REGISTRY_PATH:main -t $REGISTRY_PATH:v1.1.0 .
          
          docker push $REGISTRY_PATH:latest
          docker push $REGISTRY_PATH:main
          docker push $REGISTRY_PATH:v1.1.0
